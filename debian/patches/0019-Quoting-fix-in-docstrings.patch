From: "Igor B. Poretsky" <poretsky@mlbox.ru>
Date: Tue, 30 Jun 2026 06:56:23 +0300
Subject: Quoting fix in docstrings

---
 lisp/dtk-speak.el           |  2 +-
 lisp/emacspeak-advice.el    |  2 +-
 lisp/emacspeak-bookshare.el |  2 +-
 lisp/emacspeak-comint.el    |  2 +-
 lisp/emacspeak-load-path.el |  2 +-
 lisp/emacspeak-speak.el     | 34 +++++++++++++++++-----------------
 lisp/gmaps.el               |  2 +-
 lisp/voice-setup.el         |  2 +-
 8 files changed, 24 insertions(+), 24 deletions(-)

diff --git a/lisp/dtk-speak.el b/lisp/dtk-speak.el
index b0e14e0..6bde58d 100644
--- a/lisp/dtk-speak.el
+++ b/lisp/dtk-speak.el
@@ -517,7 +517,7 @@ it seems some accented characters in certain contexts."
     (goto-char (point-min))))
 
 (defun dtk-handle-repeating-patterns (mode)
-  "Handle repeating patterns by replacing them with 'aw <length> char-names'"
+  "Handle repeating patterns by replacing them with `aw <length> char-names'."
   (cl-declare (special dtk-cleanup-repeats))
   (goto-char (point-min))
   (mapc
diff --git a/lisp/emacspeak-advice.el b/lisp/emacspeak-advice.el
index 020366c..2441143 100644
--- a/lisp/emacspeak-advice.el
+++ b/lisp/emacspeak-advice.el
@@ -781,7 +781,7 @@ see option emacspeak-untabify-fixes-non-breaking-space."
          (emacspeak-speak-current-field))))))
 
 (defvar emacspeak-last-message nil
-  "Holds the last output generated by the Emacs 'message function.")
+  "Holds the last output generated by the Emacs `message' function.")
 
 (defvar emacspeak-lazy-message-time (current-time)
   "Records when we last spoke a message.")
diff --git a/lisp/emacspeak-bookshare.el b/lisp/emacspeak-bookshare.el
index ac92cd1..47e5093 100644
--- a/lisp/emacspeak-bookshare.el
+++ b/lisp/emacspeak-bookshare.el
@@ -280,7 +280,7 @@ Bookshare docs.")
 
 (defun emacspeak-bookshare-api-call (operation operand &optional no-auth)
   "Make a Bookshare API  call and get the result.
-Optional argument 'no-auth says we dont need a user auth."
+Optional argument `no-auth' says we dont need a user auth."
   (cl-declare (special emacspeak-bookshare-last-action-uri))
   (setq emacspeak-bookshare-last-action-uri
         (emacspeak-bookshare-rest-endpoint operation operand no-auth))
diff --git a/lisp/emacspeak-comint.el b/lisp/emacspeak-comint.el
index 6d4e61a..ef5b326 100644
--- a/lisp/emacspeak-comint.el
+++ b/lisp/emacspeak-comint.el
@@ -82,7 +82,7 @@
 (defcustom emacspeak-comint-autospeak t
   "Says if comint output is automatically spoken.
 You can use
-  `emacspeak-toggle-comint-autospeak` bound to
+  `emacspeak-toggle-comint-autospeak' bound to
   \\[emacspeak-toggle-comint-autospeak] to toggle this
 setting."
   :group 'emacspeak-speak
diff --git a/lisp/emacspeak-load-path.el b/lisp/emacspeak-load-path.el
index 91749d8..5fae7ab 100644
--- a/lisp/emacspeak-load-path.el
+++ b/lisp/emacspeak-load-path.el
@@ -54,7 +54,7 @@
 
 (defun ems-record-interactive-p (f)
   "Predicate to test if we  record interactive calls.
- Memoizes result  via property 'emacspeak."
+Memorizes result  via property `emacspeak'."
   (cond
    ((not (symbolp f)) nil)
    ((get f 'emacspeak) t) ; already memoized 
diff --git a/lisp/emacspeak-speak.el b/lisp/emacspeak-speak.el
index ab6bd14..587bc7b 100644
--- a/lisp/emacspeak-speak.el
+++ b/lisp/emacspeak-speak.el
@@ -2227,8 +2227,8 @@ Argument STRING specifies the alphanumeric phone number."
   "Speak the line containing the mark.
 With no argument, speaks the line containing the mark--this is
 where `exchange-point-and-mark' \\[exchange-point-and-mark] would
-jump.  Numeric prefix arg 'COUNT' speaks line containing mark 'n'
-where 'n' is one less than the number of times one has to jump
+jump.  Numeric prefix arg `COUNT' speaks line containing mark `n'
+where `n' is one less than the number of times one has to jump
 using `set-mark-command' to get to this marked position.  The
 location of the mark is indicated by an aural highlight achieved
 by a change in voice personality."
@@ -2685,7 +2685,7 @@ Numeric prefix arg COUNT specifies number of lines to move."
 Speaks entire window irrespective of point.
 In general, you'll never have Emacs split the screen into more than
 two or three.
-Argument ARG determines the 'other' window to speak.
+Argument ARG determines the `other' window to speak.
 Semantics  of `other' is the same as for the builtin Emacs command
 `other-window'."
   (interactive "P")
@@ -2735,8 +2735,8 @@ Argument PROMPT specifies the prompt to display."
 ;;;###autoload
 (defun emacspeak-speak-buffer-interactively ()
   "Speak the start of, rest of, or the entire buffer.
-'s' to speak the start.
-'r' to speak the rest.
+`s' to speak the start.
+`r' to speak the rest.
 any other key to speak entire buffer."
   (interactive)
   (emacspeak-speak-buffer
@@ -2745,8 +2745,8 @@ any other key to speak entire buffer."
 ;;;###autoload
 (defun emacspeak-speak-help-interactively ()
   "Speak the start of, rest of, or the entire help.
-'s' to speak the start.
-'r' to speak the rest.
+`s' to speak the start.
+`r' to speak the rest.
 any other key to speak entire help."
   (interactive)
   (emacspeak-speak-help
@@ -2755,8 +2755,8 @@ any other key to speak entire help."
 ;;;###autoload
 (defun emacspeak-speak-line-interactively ()
   "Speak the start of, rest of, or the entire line.
-'s' to speak the start.
-'r' to speak the rest.
+`s' to speak the start.
+`r' to speak the rest.
 any other key to speak entire line."
   (interactive)
   (emacspeak-speak-line
@@ -2765,8 +2765,8 @@ any other key to speak entire line."
 ;;;###autoload
 (defun emacspeak-speak-paragraph-interactively ()
   "Speak the start of, rest of, or the entire paragraph.
-'s' to speak the start.
-'r' to speak the rest.
+`s' to speak the start.
+`r' to speak the rest.
 any other key to speak entire paragraph."
   (interactive)
   (emacspeak-speak-paragraph
@@ -2775,8 +2775,8 @@ any other key to speak entire paragraph."
 ;;;###autoload
 (defun emacspeak-speak-page-interactively ()
   "Speak the start of, rest of, or the entire page.
-'s' to speak the start.
-'r' to speak the rest.
+`s' to speak the start.
+`r' to speak the rest.
 any other key to speak entire page."
   (interactive)
   (emacspeak-speak-page
@@ -2785,8 +2785,8 @@ any other key to speak entire page."
 ;;;###autoload
 (defun emacspeak-speak-word-interactively ()
   "Speak the start of, rest of, or the entire word.
-'s' to speak the start.
-'r' to speak the rest.
+`s' to speak the start.
+`r' to speak the rest.
 any other key to speak entire word."
   (interactive)
   (emacspeak-speak-word
@@ -2795,8 +2795,8 @@ any other key to speak entire word."
 ;;;###autoload
 (defun emacspeak-speak-sexp-interactively ()
   "Speak the start of, rest of, or the entire sexp.
-'s' to speak the start.
-'r' to speak the rest.
+`s' to speak the start.
+`r' to speak the rest.
 any other key to speak entire sexp."
   (interactive)
   (emacspeak-speak-sexp
diff --git a/lisp/gmaps.el b/lisp/gmaps.el
index e975cb7..1bac50b 100644
--- a/lisp/gmaps.el
+++ b/lisp/gmaps.el
@@ -257,7 +257,7 @@ Optional argument `raw-p' returns raw JSON  object."
 
 (defun gmaps-directions-url (origin destination mode)
   "Return URL   for getting directions from origin to destination.
-Parameters 'origin' and 'destination' are  be url-encoded."
+Parameters `origin' and `destination' are  be url-encoded."
   (cl-declare (special gmaps-directions-base))
   (format gmaps-directions-base  origin destination
           mode (float-time)))
diff --git a/lisp/voice-setup.el b/lisp/voice-setup.el
index 1792971..c48d5bb 100644
--- a/lisp/voice-setup.el
+++ b/lisp/voice-setup.el
@@ -250,7 +250,7 @@ VOICE-NAME are  changed."
 
 (defmacro defvoice (personality settings doc)
   "Define voice using ACSS setting.  Setting is a list of the form
-(list paul 5 5 5 5 'all) which defines a standard male voice
+(list paul 5 5 5 5 \\='all) which defines a standard male voice
 that speaks `all' punctuations.  Once
 defined, the newly declared personality can be customized by calling
 command \\[customize-variable] on <personality>-settings. "
